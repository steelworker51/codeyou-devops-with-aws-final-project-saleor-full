name: Develop Pipeline

on:
  push:
    branches:
      - develop
  workflow_dispatch:

jobs:
  build-test-scan-push:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install analysis tools
        run: |
          python -m pip install --upgrade pip
          pip install ruff pip-audit poetry==1.8.4 poetry-plugin-export

      - name: Run Ruff
        continue-on-error: true
        working-directory: saleor-api
        run: ruff check .

      - name: Export dependencies
        working-directory: saleor-api
        run: |
          poetry export \
            --without-hashes \
            --format requirements.txt \
            --output requirements-audit.txt

      - name: Run dependency audit
        continue-on-error: true
        working-directory: saleor-api
        run: pip-audit -r requirements-audit.txt

      - name: Build Saleor API image
        run: |
          docker build \
            -t ${{ secrets.DOCKERHUB_USERNAME }}/saleor-api:develop \
            ./saleor-api

      - name: Scan image with Trivy
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ secrets.DOCKERHUB_USERNAME }}/saleor-api:develop
          format: table
          exit-code: "0"
          ignore-unfixed: true
          vuln-type: os,library
          severity: CRITICAL,HIGH

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Push develop image
        run: |
          docker push \
            ${{ secrets.DOCKERHUB_USERNAME }}/saleor-api:develop